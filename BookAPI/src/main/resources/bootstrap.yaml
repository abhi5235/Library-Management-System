server:
  port: 8095
spring:
  application:
    name: book-service
  cloud:
    consul:
      config:
        enabled: true
        default-context: application
        prefixes: config
        profile-separator: '::'
        format: YAML